char strnstr(const char *big, const char *little, size_t len)
{
    size_t i;
    size_t j;
    
    i = 0;
    j = 0;

    // Si la subcadena es vacía, retornamos el puntero a la cadena principal.

    // Iteramos sobre la cadena principal, pero respetando el límite `len`.

       // Comparamos la subcadena a partir de la posición actual.

       // Si completamos el bucle interno, significa que encontramos `needle`.

     // Si no encontramos `needle`, retornamos NULL.
     
}
